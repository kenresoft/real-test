// Inlined from the root tsconfig.json rather than "extends": "../../tsconfig.json" — apps/admin
// must stay installable as a standalone clone (see apps/admin/README.md), where a relative path
// outside this directory wouldn't exist. Mirror any change to the root tsconfig here too.
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "moduleDetection": "force",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "declaration": true,
    "declarationMap": true,
    "jsx": "react-jsx",
    "types": ["vite/client"],
    // "paths" without "baseUrl" resolves each pattern relative to this file's own directory —
    // supported since TS 4.1, so this isn't a TS7-only migration; baseUrl itself was removed
    // outright in TypeScript 7 (error TS5102), which is what forced this change.
    "paths": {
      "@/*": ["./src/*"]
    },
    "noEmit": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "test/**/*.ts", "test/**/*.tsx"]
}
